#include <ctype.h>
/**
 * _isupper - function that checks for uppercase character.
 *@c: input
 * Return: 0 or 1
*/
int _isupper(int c)
{
	if (isupper(c))
	{
	return (1);
	}
	else
	{
	return (0);
	}
}

